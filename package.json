{
  "private": true,
  "name": "ottochat",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "node-sass": "^6.0.1",
    "preact-cli": "^3.0.0",
    "sass-lint": "^1.13.1",
    "sass-loader": "10",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "socket.io-client": "^4.3.2",
    "use-global-hook": "0.1.2"
  }
}
